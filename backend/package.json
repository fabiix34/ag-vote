{
  "name": "ag-copro-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "NODE_ENV=development node --watch index.js",
    "deploy": "chmod +x deploy.sh && ./deploy.sh",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "supertest": "^7.2.2",
    "vitest": "^2.1.9"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.100.0",
    "cors": "^2.8.5",
    "docx": "^9.6.1",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
